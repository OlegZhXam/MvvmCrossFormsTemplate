<?xml version="1.0" encoding="utf-8" ?>
<resources>

<!--  <style name="MainTheme" parent="MainTheme.Base">
     As of Xamarin.Forms 4.6 the theme has moved into the Forms binary 
     If you want to override anything you can do that here. 
     Underneath are a couple of entries to get you started. 

     Set theme colors from https://aka.ms/material-colors 
     colorPrimary is used for the default action bar background 
    <item name="colorPrimary">#2196F3</item>
     colorPrimaryDark is used for the status bar 
    <item name="colorPrimaryDark">#1976D2</item>
     colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets 
    <item name="colorAccent">#FF4081</item>
  </style>-->

  <style name="MyTheme"
           parent="MyTheme.Base">
  </style>

    <!-- Base theme applied no matter what API -->
  <style name="MyTheme.Base" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!--<item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>-->

        <!--Fix black screen transitions when replacing MainPage but shows SplashScreen on backpress-->
        <!--<item name="android:windowIsTranslucent">true</item>-->

        <!--Fixing content going behind navigation bar-->
        <!--<item name="android:fitsSystemWindows">true</item>--> 

        <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
        <item name="windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="windowActionBar">false</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">#27BCBC</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">#27BCBC</item>
        <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
        <item name="colorAccent">#27BCBC</item>
        <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:datePickerDialogTheme">@style/AppCompatDialogStyle</item>
  </style>

  <style name="AppCompatDialogStyle" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorAccent">#27BCBC</item>
  </style>

  <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splashlogo</item>
    <item name="android:windowFullscreen">true</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowActionBar">false</item>
  </style>
</resources>
